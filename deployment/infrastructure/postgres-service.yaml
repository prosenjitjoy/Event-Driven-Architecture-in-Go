apiVersion: v1
kind: Service
metadata:
    name: postgres-service
spec:
    selector:
      app: postgres
    ports:
    - protocol: TCP
      port: 5432  # cluster assigned ip port
      targetPort: 5432 # pod container ip port